# This file is a placeholder for environment variables required to run this project.
# Currently, there are no environment variables needed.

# Example of what future variables might look like:
# NEXT_PUBLIC_API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
# SECRET_KEY=your_secret_key_here

# Password for protected pages/routes
PAGE_ACCESS_PASSWORD=password

# Supabase (contributions table + Storage for contributor avatars)
# 1. Run supabase/migrations/20260214000000_create_contributions.sql in SQL Editor.
# 2. Storage: create a bucket (e.g. "Contributions" or "Contribuitons"), then set it to PUBLIC
#    (Storage → bucket → Configuration → Public bucket = ON). Otherwise images won't load.
# 3. Table avatar_url must match exactly: https://<project>.supabase.co/storage/v1/object/public/<BUCKET_NAME>/<file>
#    BUCKET_NAME must match the bucket name in Storage (case-sensitive).
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role (server-only): required to read subscriptions for sending newsletters.
# Get it from Supabase Dashboard → Settings → API → service_role (secret).

SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Newsletter: run subscription table migration (supabase/migrations/20260214110000_create_subscriptions.sql)
# in Supabase SQL Editor, then set the key below for sending emails.

# Resend (https://resend.com): API key for sending weekly digest + new post emails.
RESEND_API_KEY=re_xxxx
# Optional: use your verified domain (e.g. newsletter@yourdomain.com). Default: onboarding@resend.dev
# NEWSLETTER_FROM_EMAIL=newsletter@yourdomain.com

# Secret for protecting newsletter cron/API (weekly digest + notify new post).
# Use in header: Authorization: Bearer <CRON_SECRET> or x-cron-secret: <CRON_SECRET>
CRON_SECRET=your-random-secret
